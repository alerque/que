#!/usr/bin/env zsh

set -x
set -eo pipefail

host=gitlab.alerque.com
group=onqueacademy

libdir="$(dirname $(dirname $(readlink -f $0)))/lib"
source "$libdir/gitlab.zsh"

list_group_projects onuqueacademy

# vi: ft=zsh
